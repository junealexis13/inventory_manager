{
    "builds": [{
        "src" : "inventory_manager/wsgi.py",
        "use" : "@vercel/python",
        "config" : { "maxLambdaSize": "15mb"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest"  : "inventory_manager/wsgi.py"
        }
    ]
}
